<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My Reminders - SHRS</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    .rem-card {
      border-radius: 18px;
      transition: 0.3s;
    }
    .rem-card:hover {
      transform: translateY(-5px);
      box-shadow: 0px 6px 15px rgba(0,0,0,0.1);
    }
  </style>
</head>

<body class="bg-light">
<div class="container mt-3">
    <a href="{% url 'dashboard' %}" class="btn btn-secondary">Back</a>
</div>
  <!-- Navbar -->
  <nav class="navbar navbar-dark bg-primary px-3">
    <span class="navbar-brand fs-4 fw-bold">SHRS</span>
  </nav>

  <div class="container py-4">

    <!-- Header -->
    <div class="d-flex justify-content-between align-items-center mb-4">
      <h3 class="fw-semibold">Your Reminders</h3>
      <a href="{% url 'reminder' %}" class="btn btn-success px-3">+ Add New</a>
    </div>

    <!-- Reminder Cards -->
    <div class="row g-3">

      <!-- Example Reminder Card -->
      <div class="col-lg-4 col-md-6">
        <div class="card rem-card shadow-sm p-3">
          <div class="d-flex justify-content-between align-items-start">
            <h5 class="fw-bold">💊 Morning BP Tablet</h5>
            <span class="badge bg-primary">Daily</span>
          </div>
          <p class="text-muted mt-2 mb-1">Category: Medication</p>
          <p class="mb-1"><strong>Time:</strong> 8:00 AM</p>
          <p class="mb-3"><strong>Date:</strong> 2025-11-20</p>

          <div class="d-flex justify-content-end gap-2">
            <a href="{% url 'edit' %}" class="btn btn-warning btn-sm">Edit</a>
            <a href="{% url 'delete' %}" class="btn btn-danger btn-sm">Delete</a>
          </div>
        </div>
      </div>

      <!-- Another Example Card -->
      <div class="col-lg-4 col-md-6">
        <div class="card rem-card shadow-sm p-3">
          <div class="d-flex justify-content-between align-items-start">
            <h5 class="fw-bold">💧 Drink Water</h5>
            <span class="badge bg-success">Every 2 Hours</span>
          </div>
          <p class="text-muted mt-2 mb-1">Category: Water Intake</p>
          <p class="mb-1"><strong>Time:</strong> 10:00 AM</p>
          <p class="mb-3"><strong>Date:</strong> 2025-11-20</p>

          <div class="d-flex justify-content-end gap-2">
            <a href="{% url 'edit' %}" class="btn btn-warning btn-sm">Edit</a>
            <a href="{% url 'delete' %}" class="btn btn-danger btn-sm">Delete</a>
          </div>
        </div>
      </div>

    </div>
  </div>
          
</body>
</html>
